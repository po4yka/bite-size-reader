# Codex Design Skills

These `i-*` skills are manual mirrors of `.claude/skills/i-*` (Claude Code) adapted to Codex CLI syntax (`$skill-name` instead of `/skill-name`, no `user-invocable` field).

**Source of truth: `.claude/skills/i-*`.** Apply edits in both locations until a sync mechanism (symlink or generator script) is added.
