"""gRPC integration package.

Experimental: not wired into any production entry point (bot.py, app/api/main.py).
This package was scaffolded but never deployed. Treat as in-progress / future work.
"""
