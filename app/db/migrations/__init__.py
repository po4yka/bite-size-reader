"""Deprecated compatibility package — do not use.

Schema migrations are owned by Alembic (``app/db/alembic/``).
Run ``alembic upgrade head`` or ``python -m app.cli.migrate_db`` to apply migrations.
The legacy runner in ``app/cli/migrations/`` is deprecated and must not be run.
"""
