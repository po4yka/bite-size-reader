# M2 Migration Implementation Notes

This document tracks concrete repository artifacts delivered for migration milestone M2.

## M2 — Data & Contract Layer in Rust

Implemented artifacts:

- Rust summary-contract service/CLI:
  - `rust/crates/bsr-summary-contract/src/lib.rs`
  - `rust/crates/bsr-summary-contract/src/main.rs`
- Backend toggle bridge in Python:
  - `app/core/summary_contract_impl/rust_backend.py`
  - `app/core/summary_contract.py` delegates by `SUMMARY_CONTRACT_BACKEND`
- Fixture corpus for Python ↔ Rust parity:
  - Inputs: `docs/migration/fixtures/m2_summary_contract/input/*.json`
  - Expected baselines (generated from Python): `docs/migration/fixtures/m2_summary_contract/expected/*.json`
  - Fixture generator/checker: `scripts/migration/generate_m2_contract_fixtures.py`
- M2 suite entrypoint:
  - `scripts/migration/run_m2_parity_suite.sh`
  - `make m2-parity-suite`
- Rust parity and SQLite compatibility tests:
  - `rust/crates/bsr-summary-contract/tests/m2_fixture_parity.rs`

## Backend Selection

`SUMMARY_CONTRACT_BACKEND` controls runtime behavior:

- `rust` (required): summary contract execution path is Rust-only.
- Legacy values (`auto`, `python`) are accepted for compatibility but ignored.
- Python fallback for this slice is decommissioned.

Optional binary override:

- `SUMMARY_CONTRACT_RUST_BIN=/abs/path/to/bsr-summary-contract`

## SQLite Compatibility Scope

M2 validates Rust compatibility against the existing SQLite contract shape by:

- Schema checks for required core columns in `requests` and `summaries`.
- Transactional write/read smoke test (`sqlite_roundtrip_smoke`) with rollback.
- Snapshot compatibility test against SQLite snapshot candidates (prefers `app_backup.db`, falls back to `data/app.db` and latest `data/backups/app-*.db`).

## CI Coverage

- New CI job: `Migration Contract Parity (M2)` in `.github/workflows/ci.yml`.
- Verifies fixture baselines are up to date and runs Rust M2 parity tests.
